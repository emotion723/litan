// src/world/GameConstants.ts
export const PX_PER_CHI = 40;

export const BASE_SIZE = 1920;
export const ARENA_CENTER = BASE_SIZE / 2;

export const ARENA_SPACING = 480;

// 幾何尺寸（與你 BattleArena 現在一致）
export const CIRCLE_RADIUS_PX = 23.5 * PX_PER_CHI;    // 940
export const SQUARE_SIDE_PX = 44 * PX_PER_CHI;        // 1760
export const SQUARE_HALF_PX = SQUARE_SIDE_PX / 2;     // 880

// 雷射 / 其他常用距離
export const CHI_20_PX = 20 * PX_PER_CHI;             // 800
